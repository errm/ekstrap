#!/usr/bin/env ruby

require "/usr/local/lib/k8s_node_info"
include K8sNodeInfo

def run!
  set_name_tag
end

def set_name_tag
  return if value_from_tag("Name") == name_tag
  instance.create_tags(tags: [{key: "Name", value: name_tag}])
end

run!
